import 'package:flutter/material.dart';

class CardEmailPassword extends StatelessWidget {

  const CardEmailPassword({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
      return Scaffold(
          appBar: AppBar(title: const Text(''),),
          body: Container(
           
          ),
      );
  }
}